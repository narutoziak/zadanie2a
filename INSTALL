HOW TO INSTALL THIS PROGRAM:

1. Utworz katalog do ktorego chcesz wypakowac program (polecenie mkdir 'nazwa_folderu')
2. Do rozpakowania pliku uzyj polecenia tar -xzvf program.tar.gz -C /'nazwa_folderu'
3. W celu poprawnego rozpakowania i dzialania programu wejdz do rozpakowanego pliku i w terminalu wprowadz polecenie autoreconf -i
4. Pojawi sie komunikat o braku folderow i aclocal wpisz polecenie
aclocal
5. Nastepnie autoconf
6. Dalej automake
7. ./configure (Nalezy upewnic sie ze uprzednio posiada sie zainstalowany kompilator
8. Wpisujemy make
9. Program powinien zostac poprawnie skompilowany, aby go otworzyc w termianlu wpisujemy ./src/main

